version: "3.9"
services:
  mailinsight:
    build: .
    container_name: mailinsight
    ports:
      - "8000:8000"
    volumes:
      - ~/.mailinsight:/secrets    # Gmail credentials/token
    env_file:
      - .env
    restart: unless-stopped
